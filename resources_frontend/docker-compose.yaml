version: '3.8'
services: 
    main:
        build: 
            context: .
            dockerfile: Dockerfile
        command: 'npm run serve'
        volumes:
            - /app/node_modules
            - .:/app
        ports:
            - 8080:8080